<div class="event-photo buffer-both">
  <%= link_to image_tag(photo.photo.thumb.url), photo.photo.url %>

  <%= t('events.photos.added') %>
  <%= render partial: 'users/user', object: photo.user %>
</div>
